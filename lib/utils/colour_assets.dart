import 'package:flutter/material.dart';

class ColourAssets {
  // when retrieving hex from coolers.co, add 'ff' in most significant bit
  static const Color red = Color(0xff70D6FF);
  static const Color blue = Color(0xffFF70A6);
  static const Color green = Color(0xffFF9770);
  static const Color yellow = Color(0xffffd670);

  static const List<Color> all = [red, blue, green, yellow];
}
